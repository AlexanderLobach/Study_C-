using System;

public class Task_6
{
    public static double Liters(double hours)
    {
        double liters = hours * 0.5;
        int liter = Convert.ToInt32(liters);
        liters = liter;
        Console.WriteLine(liters);
        return liters;
    }
}
